import React from 'react';
import { Typography, Box } from '@mui/material';

const ReplicasPage = () => {
    return (
        <Box>
            <Typography variant="h4" gutterBottom>
                Réplicas
            </Typography>
            <Typography>
                Conteúdo da página de réplicas.
            </Typography>
        </Box>
    );
};

export default ReplicasPage;
