import React from 'react';
import { Typography, Paper } from '@mui/material';

const RelatorioSemanal = () => {
    return (
        <Paper sx={{ p: 2 }}>
            <Typography variant="h4">Relatório Semanal</Typography>
            <Typography>Conteúdo do relatório semanal.</Typography>
        </Paper>
    );
};

export default RelatorioSemanal;
